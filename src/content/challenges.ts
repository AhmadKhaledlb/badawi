import type { Challenge } from '@/domain/challenge';

// Real, curriculum-assigned Challenges go here — each belongs to exactly
// one Unit (src/domain/challenge.ts). None exist yet: no authoritative
// challenge-to-unit assignment is documented in the repository.
//
// The locked V1 challenge COUNT (28) is tracked separately as unresolved
// placeholders — see unresolved-challenge-placeholders.ts — until real
// curriculum content and unit assignments are authored.
export const challenges: Challenge[] = [];
